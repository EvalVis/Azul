[project]
name = "azul"
version = "1.0.0"
description = "Library for enabling Azul game being used by API and AI agents"
readme = "README.md"
requires-python = ">=3.12"

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
